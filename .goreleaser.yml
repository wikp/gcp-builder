builds:
  - binary: gcp-builder
    goos:
      - windows
      - darwin
      - linux
    goarch:
      - amd64

archive:
  format: tar.gz
  replacements:
    amd64: x86_64
    darwin: darwin
  files:
    - README.md
